version: "2"
sql:
  - engine: postgresql
    schema: db/schema.sql
    queries: db/queries
    gen:
      go:
        package: postgresdb
        out: internal/postygres/sqlc
        sql_package: pgx/v5
        emit_empty_slices: true
