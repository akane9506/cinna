<b>Cinna 指令</b>

<code>/help</code> 显示此帮助信息。

<code>/memory</code> 查看压缩过的对话记忆

<code>/notify &lt;status&gt;</code> 打开或关闭每日定时提醒，输入参数为on或off。
示例：
<code>/notify on</code> 打开每日提醒
<code>/notify off</code> 关闭每日提醒
